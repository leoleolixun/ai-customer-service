# V1.0 版本验收记录

## 1. 基本信息

| 项目 | 内容 |
| --- | --- |
| 版本 | `V1.0` |
| 验收日期 | `2026-07-17` |
| Git Commit | `331cf445fed87bad0d9ee3ba4e990695ecbe1ffc` |
| 构建产物 | `ai-customer-service:331cf44` |
| 镜像摘要 | `sha256:5263cf26ab4bc015180bea6db163bf7b14844c9eebf8b886bd67a387c2be323d` |
| 验收环境 | Docker 29.3.1、Python 3.12.13、PostgreSQL 17.10 + pgvector、Redis 7.4.9、MinIO RELEASE.2025-09-07T16-13-09Z |
| 验收人 | Codex（自动化部分）；独立人工评审人待填写 |

本记录只验收 V1.0 多租户 AI 客服核心。博客内容源、商城实时工具、OpenAPI/MCP
连接器和商业化能力分别属于后续版本，不是本次验收范围。

## 2. 范围确认

- 本版开发范围：[`version-roadmap.md` 的 V1.0 章节](../version-roadmap.md#4-v10多租户-ai-客服核心)。
- 详细范围和门槛：[`v1-development-plan.md`](../v1-development-plan.md)。
- 本版明确不做：博客或商城接入、真实业务工具、写操作、多渠道、完整工单、计费和连接器市场。
- 与冻结范围的差异：无。
- 验收工作区：本机隔离目录 `/tmp/ai-cs-v1-acceptance`，其中的密钥、运行日志和原始报告不提交 Git。

## 3. 构建和迁移

| 检查项 | 命令或证据 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| 后端与前端构建 | 冻结镜像 `ai-customer-service:331cf44` | 成功 | 镜像摘要与冻结 Commit 一致，API 和 Worker 使用同一镜像启动 | 通过 |
| 生产 Compose | `docker compose --env-file .env.production.example -f deploy/compose.production.yml config --quiet` | 配置有效 | 退出码 `0` | 通过 |
| 空库迁移 | 隔离空库执行 `uv run alembic upgrade head` | 到达 Head | `9c2e7f1a4b60` | 通过 |
| 迁移升级 | 从迁移链上一版本升级到 Head | 到达 Head | `9c2e7f1a4b60` | 通过 |
| 当前验收库 | 查询 `alembic_version` | 与 Head 一致 | `9c2e7f1a4b60` | 通过 |
| 备份校验 | `sha256sum -c SHA256SUMS` | 全部匹配 | PostgreSQL Dump、23 个 MinIO 对象和元数据全部匹配 | 通过 |
| 恢复演练 | `backup-20260716T221027Z-1` | 数据库和对象可恢复 | 修改数据库、Redis 和 MinIO 哨兵后恢复成功，恢复后 Ready 与双租户 Smoke Test 通过 | 通过 |

备份元数据记录的 Commit 为 `331cf445fed87bad0d9ee3ba4e990695ecbe1ffc`，应用镜像为
`ai-customer-service:331cf44`，Alembic Revision 为 `9c2e7f1a4b60 (head)`。

## 4. 自动化测试

| 测试层 | 命令 | 通过/总数 | 结果 |
| --- | --- | ---: | --- |
| Ruff Lint | `uv run ruff check .` | `1/1` | 通过 |
| Ruff Format | `uv run ruff format --check .` | `138/138` 个文件 | 通过 |
| mypy | `uv run mypy app scripts examples tests` | `128/128` 个源文件 | 通过 |
| 后端单元/API/契约测试 | `uv run pytest` | `80/80` | 通过 |
| PostgreSQL 集成测试 | 设置隔离 `TEST_POSTGRES_URL` 后执行 `uv run pytest` | `2/2`，完整结果 `82 passed` | 通过 |
| 前端类型检查 | `npm run typecheck` | `4/4` 个 Workspace | 通过 |
| 前端单元测试 | `npm run test:frontend` | `12/12` | 通过 |
| 核心 E2E | `npm run test:e2e` | `8/8` | 通过 |
| Secret 扫描 | `uv run python scripts/check_secrets.py` | `1/1` | 通过 |
| RAG 数据集校验 | `uv run python scripts/evaluate_rag.py validate` | `150/150` | 通过 |
| OpenAPI 快照 | 导出结果与 `openapi/openapi.json` 比较 | 无未说明差异 | 通过 |

E2E 覆盖管理员登录和临时密码修改、Widget 引用回答、明确拒答、用户申请转人工、客服接管、人工回复、
关闭会话以及 375px 移动视口。

## 5. V1.0 专项验收

| 验收项 | 门槛 | 实际结果 | 证据 | 结果 |
| --- | --- | --- | --- | --- |
| 两个中立演示租户 | 独立应用、知识库和会话 | `demo-retail`、`demo-saas` 完成完整 Smoke Test | `two-tenant-smoke.json`，SHA256 `cad385ec...6a558` | 通过 |
| 跨租户访问 | 成功数 `0` | API、凭据、会话、引用和检索跨租户访问成功数 `0` | 双租户 Smoke Test、后端隔离测试和 PostgreSQL 检索集成测试 | 通过 |
| RAG 数据集 | 不少于 150 条 | 150 条、5 类、22 个知识源，预测覆盖率 `100%` | `rag-dataset-validation.json`、`rag-auto-report.json` | 通过 |
| `Recall@20` | `>= 90%` | `100%` | `rag-auto-report.json` | 通过 |
| `Hit@5` | `>= 85%` | `100%` | `rag-auto-report.json` | 通过 |
| 关键事实正确率 | `>= 90%` | `99.1071%` | `rag-auto-report.json` | 通过 |
| 引用支持率（自动规则） | `>= 95%` | `98.8889%` | `rag-auto-report.json` | 通过 |
| 无答案正确拒答率 | `>= 95%` | `100%` | `rag-auto-report.json` | 通过 |
| 应转人工识别率 | `>= 95%` | `100%` | `rag-auto-report.json` | 通过 |
| 自动严重错误 | `0` | `0` | `rag-auto-report.json` | 通过 |
| 独立人工复核 | 至少 30 条并覆盖 5 类 | 已生成 30 条抽样工作表，已复核 `0` 条 | `human-review.jsonl` | **不通过** |
| 真实 Provider Smoke Test | 真实 OpenAI-compatible 模型连通、流式输出并记录首 Token P95 | 验收库仅有 2 个 Fake Provider，真实 Provider 为 `0` | 数据库 Provider 统计；尚无真实 Provider 报告 | **不通过** |
| 真实模型 Widget 闭环 | 激活同一真实模型并完成一次带引用 Widget 问答 | Fake Provider 的真实 Widget 链路通过；真实模型尚未配置 | Widget Host 与 E2E 记录 | **不通过** |
| 中立站点接入 | 30 分钟内且不修改核心代码 | 独立 Widget Host 完成 Customer Token、会话、引用和转人工接入 | `examples/widget_host` 与双租户 Smoke Test | 通过 |

RAG 自动报告 SHA256 为 `c60ed42748bee4ee95e8791aa294496c37fce5fe7172f010644a8c218935248a`。
自动指标不能替代人工对事实与引用内容的判断，因此 `release_gate.passed` 当前为 `false`。

## 6. 安全、隔离和性能

| 检查项 | 门槛 | 实际结果 | 结果 |
| --- | --- | --- | --- |
| 真实 Secret 泄漏 | `0` | Secret 扫描通过；报告、错误响应和仓库未发现真实 Secret | 通过 |
| 生产安全配置 | Debug、默认密码、匿名管理入口关闭 | `APP_ENV=production`、`APP_DEBUG=false`，启动时校验生产 Secret 和管理员密码 | 通过 |
| 混合检索 P95 | `< 500ms` | `174.794ms`，100 次，10,000 Chunk | 通过 |
| 普通管理 API P95 | `< 300ms` | `1.913ms`，100 次 | 通过 |
| 20 路并发 SSE | 串流、状态错乱或泄漏为 `0` | 20 路全部完成，20 个唯一会话和消息，串流数 `0` | 通过 |
| 真实模型首 Token P95 | 目标 `< 5s`，或记录供应商原因 | 未执行真实 Provider Smoke Test | **不通过** |
| Worker 中断恢复 | 可继续或安全重试 | 中断、重启、重投和幂等回放通过 | 通过 |
| 人工模式 AI 自动回复 | `0` | `0` | 通过 |
| 并发接管 | 只能有一个客服成功 | PostgreSQL 原子接管集成测试通过 | 通过 |

性能报告 SHA256 为 `ab315bd56d679030313e969a59d8b589ce7de7997ad3c416e3d18f325e652446`。

## 7. 文档和交付物

- FastAPI API、Celery Worker、PostgreSQL/pgvector、Redis 和 MinIO 可运行。
- 最小管理后台、客服工作台、JavaScript SDK、Widget 和中立接入示例已提供。
- OpenAPI 快照、部署、性能、评估、Worker 恢复、备份和恢复文档已提供。
- `scripts/real_provider_smoke.py` 已提供，但必须由真实凭据执行后才能形成发布证据。
- 博客同步和商城工具不属于 V1.0，不能作为本版缺陷或通过证据。

## 8. 已知限制和遗留问题

- V1.0 每个应用只允许一个活动 Chat 模型，每个知识库固定一个 Embedding 模型和版本。
- V1.0 不提供 OCR、扫描 PDF、自动模型路由、完整工单、SLA 或业务写操作。
- Redis 不保存需要长期恢复的业务事实；当前备份不包含 Redis，恢复后由数据库和幂等任务重建短期状态。

以上限制均属于冻结范围内的明确约束，不替代第 9 节列出的复验条件。

## 9. 最终结论

- 结论：**不通过**。
- 是否允许发布 Tag：**否**。
- 当前冻结版本的自动化、迁移、隔离、性能、恢复和 Fake Provider 全链路已经通过。
- 未通过原因：缺少真实 OpenAI-compatible Provider 发布前 Smoke Test、真实模型 Widget 闭环，以及至少
  30 条独立人工复核。
- 复验条件：配置真实 Provider 后运行 `scripts/real_provider_smoke.py` 并完成 Widget 带引用问答；独立
  评审人完成 30 条工作表后合并结果，使用 `--enforce-gate` 重新评分。
- 上述门禁全部通过后，更新本记录的真实指标、验收人和最终结论，再发布 `v1.0.0` Tag。
